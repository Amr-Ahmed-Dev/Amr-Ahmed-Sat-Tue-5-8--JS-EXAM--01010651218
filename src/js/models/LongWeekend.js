export default class LongWeekend {
  constructor(startDate, endDate, dayCount, needBridgeDay) {
    this.startDate = startDate;
    this.endDate = endDate;
    this.dayCount = dayCount;
    this.needBridgeDay = needBridgeDay;
  }
}
